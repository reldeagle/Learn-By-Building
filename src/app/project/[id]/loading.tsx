import { RouteLoading } from "@/components/route-state";

export default function Loading() {
  return (
    <RouteLoading
      description="Loading the goal, requirements, and your workspace."
      title="Opening your project"
    />
  );
}
